{
  "name": "firehose-test",
  "version": "0.0.1",
  "description": "Creation of Amazon Kinesis Firehose Delivery Stream example",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "firehose",
    "amazon"
  ],
  "dependencies": {
    "aws-sdk": "^2.2.41",
    "dotenv": "^8.2.0"
  }
}
